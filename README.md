make sure you have cloudflared tunnel installed
and run this code to make sure you have all dependencies intsalled
``pip install flask flask-socketio``

run ``startup.py`` to start the program, and only get the second key not the first one
